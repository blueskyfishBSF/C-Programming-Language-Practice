#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>

int get_max(int x, int y)
{
	if (x > y)
		return x;
	else
		return y;
}

int main()
{
	//ret_type fun_name(paral，*)
	//statement;//语句项
	//ret_type 返回类型
	//fun_name函数名
	//paral函数参数
	return 0;
}